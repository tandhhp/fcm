export enum InvoiceStatus {
    Pending = 0,
    Approved = 1,
    Rejected = 2,
    Cancelled = 3,
    SAConfirmed = 4
}

export enum PaymentMethod {
    BankTransfer = 0,
    Card = 1,
    Cash = 2
}

export enum BillStatus
{
    Pending,
    Approved,
    Rejected,
    Cancelled
}