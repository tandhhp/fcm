export enum InvoiceStatus {
    Pending = 0,
    Approved = 1,
    Rejected = 2
}

export enum PaymentMethod {
    BankTransfer = 0,
    Card = 1,
    Cash = 2
}

export enum BillStatus
{
    Pending,
    Approved,
    Rejected,
    Cancelled
}